#ifndef EDGE_FIND
#define EDGE_FIND

#include "position.h"

position* generate_edges(const position *_vertex_array, const int _vertex_count){
    position *out = nullptr;
    for (int i = 0; i<_vertex_count; i++){
        for (int j =  0; j<_vertex_count; j++){
            
        }
    }
}

#endif